# 高德地图API Key（必需）
# 请在 https://console.amap.com/dev/key/app 申请
AMAP_KEY=your_api_key_here

# API请求超时时间（毫秒，可选，默认10000）
# AMAP_TIMEOUT=10000

# Dashboard服务端口（可选，默认8080）
# DASHBOARD_PORT=8080

# Dashboard服务主机（可选，默认localhost）
# DASHBOARD_HOST=localhost
